// problem -06
// Write a function to calculate the power of a number.
// Hint: Use a loop to multiply the base n times or use Math.pow(base, exp).
// Sample Input/Output:

function number(num,pow){
    return(Math.pow(num,pow))
}
console.log(number(2,9))